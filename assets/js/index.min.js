function bin2Dex(bin){let decimal=0;for(var ndex=bin.length-1;index>=0;index--)decimal+=parseInt(bin[index])*Math.pow(2,bin.length-1-index);return decimal}window.addEventListener("load",(function(event){querySelector("#btnConverter").onclick=function(){}}));